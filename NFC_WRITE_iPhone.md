# iPhone で NFC タグに書き込む手順

書き換え可能 NFC タグに **`https://ambit.go2020.tokyo/card/`** を書き込む。
iPhone 単体で完結する手順。

---

## 対応機種

| 機種 | NFC 読み取り | NFC 書き込み |
|---|---|---|
| iPhone 7 以降 | ✅ アプリ経由 | ✅ アプリ経由 |
| iPhone XS 以降 | ✅ **背面に近づけるだけで自動** (Background Tag Reading) | ✅ アプリ経由 |
| iPhone 6s 以前 | ❌ | ❌ |

**iPhone XS 以降**であれば、書き込んだタグはアプリを起動しなくても、
ロック画面のままタグに近づけるだけで読み取り通知が出る。

---

## 1. NFC 書き込みアプリをインストール

App Store で以下のいずれかをインストール(無料):

### 推奨: **NFC Tools** (by wakdev)
- App Store: <https://apps.apple.com/jp/app/nfc-tools/id1252962749>
- シンプルで広告少なめ。NDEF レコード書き込みに対応。

### 代替: **NXP TagWriter**
- NFC タグの大手メーカー NXP 公式
- 機能豊富だが UI はやや古め

---

## 2. NFC Tools で書き込む(推奨手順)

### 2-1. アプリを起動

下部タブから **「書き込む (Write)」** を選択。

### 2-2. レコード追加

**「+ レコードを追加」** をタップ → **「URL / URI」** を選択。

### 2-3. URL を入力

```
https://ambit.go2020.tokyo/card/
```

入力したら **「OK / 確認」**。

### 2-4. 書き込み実行

「**書き込む / Write**」ボタンをタップ → iPhone 上部にダイアログが出る。

### 2-5. iPhone をタグにかざす

iPhone の **上部背面(カメラの少し下あたり)** を NFC タグに数秒近づける。

- 「書き込み成功 / Written」と表示されれば完了
- 失敗したら少し位置をずらして再試行

---

## 3. 書き込みの確認

### A. NFC Tools で「読み取り (Read)」

下部タブの **「読む / Read」** を開いてタグにかざす。
書き込んだ URL が表示されれば OK。

### B. 別の iPhone / Android でかざす

ロック解除した状態の iPhone XS 以降を背面でかざす(またはコントロールセンターから NFC リーダを起動)。
画面上部に **「Safari で開く」 (URL: ambit.go2020.tokyo/card/)** という通知が出れば成功。

タップで名刺ページが Safari で開く。

---

## 4. タグを書き換え不可にロックする(任意)

**書き換え可能なまま運用するのを強く推奨**(URLを後から変更可能なため)。

どうしても改ざん防止したい場合のみ:

- NFC Tools の **「その他 / Other」** → **「タグをロック (Lock tag)」**
- ⚠️ **一度ロックすると物理的に二度と書き換え不可** になる
- 紛失や URL 変更に備え、最低 2 枚は書き換え可能のまま残すべき

---

## 5. よくあるトラブル

### 書き込みに失敗する
- タグの位置を **iPhone 上部背面(カメラレンズの少し下)** に合わせる
- ケースが厚い場合は外して試す
- 金属の上にタグを置くと読み取れない(下に紙を挟むなど)
- タグの **容量不足** の可能性 → 短縮 URL(後述)を使う

### 書き込んだのに iPhone が反応しない
- iPhone XS 以前なら **背面自動読み取りは非対応** → NFC Tools の「読む」から
- iPhone XS 以降でも **Wi-Fi/モバイルデータがオフ** だと URL 解決に失敗
- 機内モードを解除

### タグが既に書き込み済みで上書きできない
- 元の書き込み元アプリで「ロック」されている可能性
- 別のタグを使う

---

## 6. 短縮 URL を併用する(推奨オプション)

NFC タグ(特に NTAG213)は容量が **約 137 バイト** しかない。
今の URL は 33 文字なので問題なく入るが、将来 UTM パラメータなどを足す予定があれば
短縮 URL にしておくと安全:

- Bitly / TinyURL などで短縮: 例 `https://bit.ly/akiyama-card`
- 自前ドメインで 301 リダイレクトを設定するのが理想:
  - 例: `https://ambit.go2020.tokyo/c` → `https://ambit.go2020.tokyo/card/` に Apache RewriteRule

短縮 URL ならタグ容量を圧迫せず、リンク先を Apache 側で柔軟に切り替えられる。

---

## 7. 推奨タグ種別

| タグ | 容量 | 互換性 | 価格 |
|---|---|---|---|
| **NTAG213** | 137 B | iOS/Android 両対応 ◎ | 1枚 50-100円 |
| **NTAG215** | 504 B | 同上、容量大きい | 1枚 100-200円 |
| **NTAG216** | 888 B | 同上、最大容量 | 1枚 200円+ |

すでに用意済みのタグが何かは裏面/パッケージで確認。
通常の URL 用途であれば **NTAG213** で十分。

---

## 8. 運用フロー(まとめ)

```
[lib/profile.js を編集] (名前・肩書・SNSリンクなど)
        ↓
[./deploy.sh] (ローカルから VPS に rsync)
        ↓
[https://ambit.go2020.tokyo/card/] が即更新

NFC タグの中身は URL 1個だけなので、内容変更してもタグ書換は不要。
```

つまり一度 NFC タグに URL を書き込めば、**プロフィール内容の修正でタグ自体を触る必要はない**。
これが NFC + Web 名刺のメリット。

---

**最終更新**: 2026-05-28
